CREATE TABLE Person(ID INT NOT NULL,
                    Name VARCHAR(255),
                    Salary INT,
                    PRIMARY KEY(ID)
              );

insert into Person values(1, "Rick", 123);
insert into Person values(2, "Shelby",100);
insert into Person values(3, "MrBulldops",200);

select ID, Salary from Person where Salary > 125;

select * from Person;
