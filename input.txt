CREATE TABLE Person(ID INT NOT NULL,
                    Name VARCHAR(255),
                    PRIMARY KEY(ID));

insert into Person values(1, "Rick");
insert into Person values(2, "Shelby");
insert into Person values(3, "MrBulldops");

delete from Person where ID = 3;

select * from Person where ID < 4;
